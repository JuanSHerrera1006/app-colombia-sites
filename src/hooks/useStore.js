import { useReducer } from 'react'

const initialState = {
  siteInfo: [],
  initialPoint: '',
  finalPoint: '',
}

function reducer(state, action) {
  const { type } = action
  switch (type) {
    default:
      break
  }
  return
}
